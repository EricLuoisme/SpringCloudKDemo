server:
  port: 10088
spring:
  application:
    name: grpc-server # for grpc
  profiles:
    active: local
  # Nacos配置
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
#grpc:
#  server:
#    port: 9991